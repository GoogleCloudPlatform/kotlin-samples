{
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "echo 'The build will take a few minutes and not output anything'",
        "./gradlew --no-daemon --console=plain bootBuildImage --imageName=$IMAGE_URL >/dev/null"
      ]
    }
  }
}